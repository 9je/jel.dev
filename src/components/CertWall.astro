---
import { getCollection } from 'astro:content';
interface Props { size?: 'wall' | 'hall' }
const { size = 'wall' } = Astro.props;
const certs = await getCollection('certs');
---
<section class="wall" data-cert-wall data-size={size} aria-labelledby="certs-h">
  <h3 id="certs-h" class="wall-title">Certifications</h3>
  <ul class="wall-list">
    {certs.map(({ data: c }) => (
      <li class="badge">
        <img src={c.badgeImage} alt="" width="96" height="96" loading="lazy" />
        <span class="badge-name">{c.verifyUrl ? <a href={c.verifyUrl} rel="noopener">{c.name}</a> : c.name}</span>
        <span class="badge-issuer">{c.issuer}</span>
      </li>
    ))}
  </ul>
</section>

<style>
  .wall { margin-top: 3rem; }
  .wall-title { font-size: 1.3rem; margin-bottom: 1rem; }
  .wall-list {
    list-style: none; margin: 0; padding: 0;
    display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 1rem;
  }
  .badge {
    display: grid; justify-items: center; gap: 0.35rem;
    padding: 1rem 0.5rem;
    background: var(--surface);
    border-top: 1px solid var(--surface-edge);
    border-radius: var(--radius);
    text-align: center;
  }
  .badge img { width: 96px; height: 96px; object-fit: contain; }
  .badge-name { font-weight: 600; font-size: 0.95rem; }
  .badge-issuer { color: var(--text-dim); font-size: 0.85rem; }
  .wall[data-size='hall'] .wall-list { grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 1.5rem; }
  .wall[data-size='hall'] .badge img { width: 160px; height: 160px; }
  .wall[data-size='hall'] .wall-title { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); }
</style>
